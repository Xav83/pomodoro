#pragma once

#include <filesystem>

namespace pomodoro::files {
namespace sounds {
extern std::filesystem::path start_break;
extern std::filesystem::path start_work;
} // namespace sounds

extern std::filesystem::path configuration;
} // namespace pomodoro::files
